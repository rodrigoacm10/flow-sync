import { OrderService } from '@/services/orderService'

export async function GET() {
  try {
    // pegar o userId do cookie
    // const resposne = await new OrderService().list()
  } catch (err) {}
}
